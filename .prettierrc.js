module.exports = {
    // 객체 또는 배열이 여러 줄로 구성되어있으면 맨 마지막 줄에 쉼표를 붙인다.
    trailingComma: 'all',
    // 프린터가 줄 바꿈 할 줄의 길이 지정한다.
    printWidth: 80,
    // 공백 대신 탭으로 줄을 들여 쓴다.
    useTabs: false,
    // 탭 사이즈를 지정한다.
    tabWidth: 4,
    // 큰 따음표 대신 작은 따옴표 사용한다. (JSX는 무시)
    singleQuote: true,
    // 객체 리터럴에서 대괄호 사이에 공백을 넣는다.
    bracketSpacing: true,
    // 명령문 끝에 세미콜론을 넣는다.
    semi: true,
    // 단독 화살표 함수 매개 변수에 괄호를 넣는다.
    arrowParens: 'always',
};
